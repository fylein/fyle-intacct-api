apiVersion: v1
kind: Service
metadata:
  name: intacct-api
  namespace: integrations
  labels:
    app: intacct-api
spec:
  ports:
    - port: 8000
  selector:
    app: intacct-api
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: intacct-api
  namespace: integrations
  labels:
    app: intacct-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: intacct-api
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: intacct-api
    spec:
      containers:
        - name: intacct-api
          image: docker.io/{{DOCKERHUB_USERNAME}}/fyle_intacct-api:{{RELEASE_VERSION}}
          resources:
            limits:
              memory: 500Mi
            requests:
              memory: 250Mi
          env:
            - name: GET_HOSTS_FROM
              value: dns
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /api/workspaces/ready/
              port: 8000
            initialDelaySeconds: 10
            timeoutSeconds: 1
          envFrom:
            - secretRef:
                name: intacct-api-secrets
      imagePullSecrets:
        - name: myregistrykey2
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: intacct-api-qcluster
  namespace: integrations
  labels:
    app: intacct-api-qcluster
spec:
  replicas: 1
  selector:
    matchLabels:
      app: intacct-api-qcluster
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: intacct-api-qcluster
    spec:
      containers:
        - name: intacct-api
          image: docker.io/{{DOCKERHUB_USERNAME}}/fyle_intacct-api:{{RELEASE_VERSION}}
          command: ['/bin/bash', 'start_qcluster.sh']
          resources:
            limits:
              memory: 800Mi
            requests:
              memory: 600Mi
          env:
            - name: GET_HOSTS_FROM
              value: dns
          envFrom:
            - secretRef:
                name: intacct-api-secrets
      imagePullSecrets:
        - name: myregistrykey2